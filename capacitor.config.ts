import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.ionicnotes.dynamicisland',
  appName: 'DynamicIsland',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
